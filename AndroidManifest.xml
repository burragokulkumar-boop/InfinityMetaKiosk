<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet access for remote management -->
    <uses-permission
        android:name="android.permission.INTERNET" />

    <uses-permission
        android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Allow Infinity Meta to detect and launch Infinity Learn -->
    <queries>
        <package
            android:name="apps.infinitylearn.lms" />
    </queries>

    <application
        android:theme="@style/AppTheme"
        android:label="Infinity Meta"
        android:icon="@drawable/infinity_meta_icon"
        android:allowBackup="false"
        android:supportsRtl="true">

        <!-- Main kiosk activity -->
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:exported="true"
            android:launchMode="singleTask"
            android:lockTaskMode="if_whitelisted">

            <intent-filter>

                <action
                    android:name="android.intent.action.MAIN" />

                <category
                    android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

        <!-- Device Administrator / Device Owner receiver -->
        <receiver
            android:name=".KioskDeviceAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true"
            android:label="@string/app_name"
            android:description="@string/app_name">

            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />

            <intent-filter>

                <action
                    android:name="android.app.action.DEVICE_ADMIN_ENABLED" />

            </intent-filter>

        </receiver>

        <!-- Remote management command receiver -->
        <receiver
            android:name=".ManagementCommandReceiver"
            android:exported="false" />

        <!-- Remote management service -->
        <service
            android:name=".ManagementService"
            android:exported="false" />

    </application>

</manifest>
